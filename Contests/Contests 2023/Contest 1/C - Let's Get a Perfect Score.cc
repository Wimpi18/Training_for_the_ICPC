#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N, M;
    string S;
    cin >> N >> M;
    int matriz[N][M];
    for (int i = 0; i < N; i++)
    {
        cin >> S;
        /* for (int j = 0; j < M; j++)
        {
            if (S[j] == 'o')
            {
                matriz[i][j] = 1;
            }
            else
            {
                matriz[i][j] = 0;
            }
        } */
    }

    /* for (int i = 0; i < N; i++)
    {
        for (int j = 0; j < M; j++)
        {
            cout << matriz[i][j];
        }
        cout << '\n';
    } */
}