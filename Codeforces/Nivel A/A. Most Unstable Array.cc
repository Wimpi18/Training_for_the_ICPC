#include <iostream>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int casos, n, m;
    cin >> casos;
    for (int c = 0; c < casos; c++)
    {
        cin >> n >> m;
        int res;
        if (n > 1)
        {
        }
        else
        {
            res = 0;
        }
        cout << res << '\n';
    }
}