#include <iostream>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int numEmpleados, pManager;
    cin >> numEmpleados;
    for (int nE = 0; nE < numEmpleados; nE++)
    {
        cin >> pManager;
    }
}