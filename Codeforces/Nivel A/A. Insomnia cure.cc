#include <iostream>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int k, l, m, n, dragones;
    cin >> k >> l >> m >> n >> dragones;
    
}