#include <iostream>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int casos, n;
    cin >> casos;
    for (int c = 0; c < casos; c++)
    {
        cin >> n;
        
    }
}